
<view class="counter {{style}}">
  <button size="mini" data-wpy-evt="9-0" bind:tap="_proxy">  +  </button>
  <button size="mini" data-wpy-evt="9-1" bind:tap="_proxy">  -  </button>
  <label class="count {{ [ num > 55 ? 'red' : '',num < 45 ? 'green' : '' ] }}"> {{num}} </label>
</view>
